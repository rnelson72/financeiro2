<?php

class BancoController extends ControllerBase {
    protected $modelClass = 'Banco';
    protected $viewPath = 'banco';

    // Aqui, caso queira, pode sobrescrever métodos
    // Ex: se banco precisar de salvar com validação adicional
}